const authenticateToken = require("../../utils/JWT/JWTAuthentication");
const isUser= require("../../utils/Middleware/isUser"); 
const express = require("express");
const router = express.Router();
const userController = require("../../controllers/Admin&UserControllers/userController");
//functionalities - to be implemented in another ticket 

module.exports= router;